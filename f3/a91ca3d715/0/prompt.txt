https://github.com/xenodium/emacs-skills あるといいとおもうんだけど、どう？

---

cluade plugin って user local にいれるって認識で合ってる？

---

ok user local にいれましょう

---

こちらに 

[@30-tabspaces-transient.el](file:///Users/mizuki-y/.dot.d/emacs.d/inits/30-tabspaces-transient.el)

<context ref="file:///Users/mizuki-y/.dot.d/emacs.d/inits/30-tabspaces-transient.el">
;;; -*- lexical-binding: t -*-
;; C-x t も project 同様2打鍵ともCtrlを離す必要があって面倒なので、
;; tab-bar/tabspaces 系のよく使うコマンドを transient にまとめ、
;; project 用 (C-M-s-p) と揃えて C-M-s-t で開けるようにする。
;; タブそのものへのジャンプは M-1..M-9 (tab-bar-select-tab-modifiers)
;; があるのでここには含めない。

(require 'transient)

(transient-define-prefix my/tabspaces-transient ()
  "Tab / workspace commands."
  [["Workspace"
    ("s" "Switch/create workspace" tabspaces-switch-or-create-workspace)
    ("w" "Open project as workspace" tabspaces-open-or-create-project-and-workspace)]
   ["Tab"
    ("t" "New tab"      tab-new)
    ("0" "Close tab"    tab-close)
    ("1" "Close others" tab-close-other)
    ("r" "Rename tab"   tab-rename)
    ("u" "Undo close"   tab-undo)]
   ["Move"
    ("o" "Next tab" tab-next)
    ("O" "Prev tab" tab-previous)]])

(global-set-key (kbd "s-t") #'my/tabspaces-transient)

</context>

---

save session current project をいれてもらっていい？

---

save current project session と save session はなにがちがう？

---

restore すると、どちらが復元されるの？

---

yes

---

swtich project で project 移動して tabspace がsession あれば自動で復元するとこまでしてほしい。いまは project 開いて restore session しないといけないから

---

project に限り自動保存するのはどう？

---

ｙ

---

C-t で consult ひらいたとき buffer に 2 2 2 と続くのはなに？ばぐってる？

---

30-tabspaces.el:35:7: Warning: ‘tab-bar-new-button-show’ is an obsolete
    variable (as of 28.1); use ‘tab-bar-format’ instead.